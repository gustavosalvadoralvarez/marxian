module.exports = function (parent){
	var parent = document.getElementById('parent'),
		buffer = [];
	return function buffered(k, v){
		parent.
		if (v){
			buffer.concat(v)
		} 
		if (buffer.length){

		}
	}
}